#!/usr/bin/env bash

set -e

[[ -z "$1" ]] && exit 1

out=$(basename "$1")
out=${out//.md/}

pandoc --latex-engine=xelatex \
--to=latex \
--number-sections \
--standalone --smart \
-o ~/"${out}.pdf" "$1"
