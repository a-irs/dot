#!/usr/bin/env bash

set -e

[[ -z "$1" ]] && exit 1

out=$(basename "$1")
out=${out//.md/}

pandoc --latex-engine=xelatex \
-V geometry:margin=2cm -V lang=german \
--to=latex \
--standalone --smart \
-o ~/"${out}.pdf" "$1"
