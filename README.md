```bash
$ git clone --recursive https://github.com/a-irs/dotfiles ~/.dot
```

To setup symlinks for installed applications:

```bash
$ ~/.dot/.setup.sh
```

To remove symlinks:

```bash
$ ~/.dot/.setup.sh rm
```
