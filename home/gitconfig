[user]
    email = alexirsi@gmail.com
    name = a-irs
[color]
    ui = 1
[alias]
    subup = submodule update --remote --merge
    p = !git add -A && git commit && git push origin master
    clone1 = clone --depth=1
    i = icdiff
    d = !git add --intent-to-add -A && git diff $@
    co = checkout
    cm = commit
    l = log
    s = status -sbu
    a = !git add --intent-to-add -A && git add --patch && git commit
