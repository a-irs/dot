snippet #!|shebang "shebang" br
#!/usr/bin/env python3

def main():
    ${1:${VISUAL}}

if __name__ == "__main__":
    main()
endsnippet


snippet di "dependency injection" b
from typing import Callable

def external_dependency_snmpwalk(oid: str, hostname: str) -> dict:
    cmd = subprocess.run(("snmpwalk", hostname, oid), capture_output=True, check=True, text=True)
    return {"stdout": cmd.stdout.splitlines(), "stderr": cmd.stderr.splitlines()}

def run(hostname: str, snmpwalk_func: Callable[..., dict] = external_dependency_snmpwalk) -> str:
    return snmpwalk_func("1.2.3", hostname).get("stdout", "")

def main():
    # use default arg for external dependency (snmpwalk)
    run(sys.argv[0])

    # in test for run(): inject function that fakes data
    run("dummy", lambda *_: {"stdout": "fake"})
endsnippet


snippet test-unittest "test unittest base"
#!/usr/bin/env python3

import unittest
import unittest.mock
import ${1:module_abc} as sut

class Test${2:ModuleAbc}(unittest.TestCase):
    def test_that_blabla(self):
        self.assertTrue(sut.run())

    @unittest.mock.patch("subprocess.run")
    def test_run_with_mock_variant_1(self, mock_run: unittest.mock.MagicMock):
        mock_run.return_value.stdout = """..."""
        mock_run.return_value.stderr = ""
        self.assertTrue(sut.run())

    def test_run_with_mock_variant_2(self):
        with unittest.mock.patch("subprocess.run") as mock_run_func:
            mock_run_func.return_value.stdout = "foo\nbar"
            mock_run_func.return_value.stderr = ""
            self.assertTrue(sut.run())

if __name__ == "__main__":
    unittest.main()
endsnippet


snippet test-pytest "test pytest base"
#!/usr/bin/env python3

import ${1:module_abc} as sut

def test_that_blabla():
    assert sut.run() == "expected"


from pathlib import Path
def test_that_includes_tmp_fixture(tmp_path: Path):
    assert sut.run() == "expected"
endsnippet


snippet test-doctest "test doctest base"
#!/usr/bin/env python3
# run with: python3 -m doctest file.py
def transform_authors(authors: str):
    """
    transforms authors string in a way that:

    >>> transform_authors("Author A; Author B")
    ('Author B', 'Author A')
    """
    return tuple(reversed([a.strip() for a in authors.split(";")]))
endsnippet


snippet "dot1|dotdict1" "DotDict read" br
# for more complex uses: https://pypi.org/project/dotmap/

class ${1:DotDict}(dict):
    """
    access a dict with dot notation: mydict.key_blabla instead of mydict["key-blabla"]
    """
    def __getattr__(self, attr):
        if attr in self:
            return self[attr]
        return self[attr.replace("_", "-")]
    def __getstate__(self):
        return self
endsnippet


snippet "dot2|dotdict2" "DotDict write" br
from types import SimpleNamespace
from typing import Any

class DotDict(SimpleNamespace):
    def __getattr__(self, name: str) -> Any:
        value = DotDict()
        setattr(self, name, value)
        return value

def dot_dict(obj: Any) -> Any:
    if isinstance(obj, dict):
        return DotDict(**{key: dot_dict(value) for key, value in obj.items()})
    if isinstance(obj, list):
        return [dot_dict(item) for item in obj]
    if isinstance(obj, tuple):
        return tuple(dot_dict(item) for item in obj)
    return obj

data = dot_dict(
    {
        "name": "Alice",
        "address": {
            "city": "NY",
        },
    }
)
data.foo.bar = ["baz", "x"]
print(data)
print(data.name)
print(data.foo.bar)
print(data.address.city)
endsnippet


snippet "exception" "Exception" br
class ${1:MyException}(Exception):
    """raised when ${2:...}"""
    pass
endsnippet


snippet match "match" b
# https://benhoyt.com/writings/python-pattern-matching/

def mysum(all: list[int]) -> int:
    match all:
        case []:
            return 0
        case [x]:
            return x
        case [x, *xs]:
            return x + sum(xs)
    return 0
endsnippet


snippet "run|subprocess|shell|exec" "subprocess" br
import subprocess

try:
    process = subprocess.run(["ls", "-l"], capture_output=True, check=True, text=True)
    print([
        process.stdout,
        process.stderr,
        process.returncode
    ])

# throws subprocess.CalledProcessError if process.returncode != 0
# throws FileNotFoundError if command is not in PATH
except subprocess.CalledProcessError as e:
    print(e.output, e.stderr, e.returncode)
endsnippet


snippet "arg|args" "argparse" br
# https://docs.python.org/3/library/argparse.html
import argparse

def cli_args() -> argparse.Namespace:
    p = argparse.ArgumentParser(description="desc 123", allow_abbrev=False)
    p.add_argument("--arg1", "-a", help="help text 1", required=True)
    p.add_argument("--enable_feature_xyz", help="toggle for %(prog)s", action="store_true", default=False)
    p.add_argument("--count", help="has to be %(type)s (default: %(default)s)", type=int, default=10)
    p.add_argument("--verbose", "-v", help="store count of used args", action="count", default=0)
    p.add_argument("--choose1", choices=["this", "that"])
    p.add_argument("--choose2", type=int, choices=range(1, 6))
    p.add_argument("--optarg", nargs="?", help="optional")
    p.add_argument("--multiarg", nargs="+", help="can be specified multiple times, at least 1")
    p.add_argument("--multiarg_opt", nargs="*", help="can be specified multiple times, at least 0")
    p.add_argument("--file_out", type=argparse.FileType("w"), default=sys.stdout)
    p.add_argument("--file_in", type=argparse.FileType("r"), default=sys.stdin)

    subparser = p.add_subparsers(dest="cmd")
    subparser.add_parser("pull")
    subparser.add_parser("list")

    return p.parse_args()
endsnippet


snippet "main" "main method" br
def main():
    ${1:${VISUAL}}

if __name__ == "__main__":
    main()
endsnippet

snippet "dc|dataclass" "dataclass" br
from dataclasses import dataclass, field

@dataclass(frozen=True)
class ${1:Point}:
    x: int
    y: int = 0
    description: str = ""
    others: list[int] = field(default_factory=list)

	# access via p.sum
	@property
	def sum(self) -> int:
		return self.x + self.y
endsnippet


snippet "raise|throw|exc" "exception" br
# https://docs.python.org/3/library/exceptions.html#base-classes
raise ValueError("Error message")
endsnippet


snippet "tmp|temp" "temporary" br
import tempfile

with tempfile.NamedTemporaryFile("w") as temp:
    temp.write("content")
    temp.flush()
endsnippet


snippet "defaultdict|dd" "defaultdict" br
import collections

d = collections.defaultdict(list)  # or set, int, ...
endsnippet


snippet "b|br|break" "breakpoint" br
from pprint import pp
breakpoint()
endsnippet


snippet "b2|br2|break2" "breakpoint old" br
import pdb; pdb.set_trace()
endsnippet


snippet "fori" "enumerate" br
for i, item in enumerate(${1:list}):
    print(f"{i}={item}")
endsnippet


snippet "fore" "iterate over iterable" br
for item in ${1:list}:
    print(f"{item}")
endsnippet


snippet "ford" "iterate over dict" br
for key, value in ${1:dict_obj}.items():
    print(f"{key}={value}")
endsnippet


snippet "class" "class" br
class ${1:ClassName}:
    def __init__(self, attr1):
		self.attr1 = attr1
    def __str__(self):
        return str("<{}: {}>".format(type(self).__name__, vars(self)))
endsnippet


snippet "jsonp" "json print" br
json.dumps(${1:item}, indent=2)
endsnippet


snippet "jsonr" "json read" br
with open("${1:filename}", "r") as fd:
    data = json.load(fd)
endsnippet


snippet "jsonw" "json write" br
with open("${1:filename}", "w") as fd:
    json.dump(data, fd)
endsnippet


snippet "csv2py" "csv to python object" br
import csv
with open("file.csv", "r") as fd:
    reader = csv.DictReader(fd, delimiter=";")
    for d in reader:
        print(d)
endsnippet


snippet decorator "decorator" b
from typing import Callable
from functools import wraps
def logged(f: Callable):
    @wraps(f)  # assigns correct __name__, __module__, __doc__ etc.
    def inner(*args, **kwargs):
        print("Calling", f.__name__)
        return f(*args, **kwargs)
    return inner

@logged
def test(): ...
endsnippet


snippet decoratorc "decoratorcontext" b
from contextlib import ContextDecorator

class track_entry_and_exit(ContextDecorator):
    def __init__(self, name):
        self.name = name

    def __enter__(self):
        logging.info('Entering: %s', self.name)

    def __exit__(self, exc_type, exc, exc_tb):
        logging.info('Exiting: %s', self.name)

# can be used as context manager
with track_entry_and_exit('loader'):
    pass

# can be used as decorator
@track_entry_and_exit('loader')
def activity():
    pass
endsnippet


snippet context "context manager class" b
# https://docs.python.org/3/library/contextlib.html
class ${1:ContextManager}:
	def __enter__(self):
		${2:pass}

	def __exit__(self, exc_type, exc_value, traceback):
		${3:pass}

`!p snip.rv=t[1].lower()` = ${1}()
with `!p snip.rv=t[1].lower()`:
	pass
endsnippet


snippet contextdecorator "context manager decorator" b
# https://docs.python.org/3/library/contextlib.html
from contextlib import contextmanager

@contextmanager
def managed():
    print("entered")
    try:
        yield
    finally:
        print("cleanup")

with managed():
    print("middle")

# https://docs.python.org/3/library/contextlib.html
@contextmanager
def managed_complex(resource: str):
    print("entered")
    try:
        yield resource.upper()
    finally:
        print("cleanup")

with managed_complex("test") as m:
    print(m)
endsnippet
