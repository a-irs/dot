#!/usr/bin/env bash

set -eu

STEP=2

action=$1
b() { xbacklight -time 100 "$@"; }

if [[ "$action" == + ]]; then
    b -inc "$STEP"
elif [[ "$action" == - ]]; then
    b -dec "$STEP"
fi

# limit to 1
if [[ "$(xbacklight -get | cut -d . -f 1)" == 0 ]]; then
    b -set 1
fi

