#!/usr/bin/env bash

set -euo pipefail

URL=$1

TMP="$(mktemp -d)"
trp() { rm -rf "$TMP"; echo "Removed '$TMP'."; }
trap trp EXIT

clear
echo ":: $URL"
echo ""
git clone "$URL" "$TMP"
cd "$TMP"
echo ""
ls -F
zsh
